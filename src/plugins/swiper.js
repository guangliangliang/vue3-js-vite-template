import 'swiper/css'
import 'swiper/css/pagination'
import 'swiper/swiper-bundle.css'

// eslint-disable-next-line no-unused-vars
export function setupSwiper(app) {
  // Swiper 主要通过组件方式使用，这里只导入样式
  // 如果需要全局配置，可以在这里添加
}
